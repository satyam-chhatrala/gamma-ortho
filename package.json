{
  "name": "gamma-ortho-backend",
  "version": "1.0.0",
  "description": "Backend for Gamma Ortho Instruments",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",  // Use current stable versions
    "nodemailer": "^6.9.1", // Use current stable versions
    "cors": "^2.8.5"       // Use current stable versions
  },
  "author": "",
  "license": "ISC"
}
